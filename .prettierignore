.next
node_modules
coverage
package-lock.json
*.md

# self-contained Lighthouse reports — generated by the CLI, not hand-written,
# and forcing tool output through Prettier just to pass format:check would
# defeat the point of keeping the report byte-for-byte as Lighthouse wrote it
docs/lighthouse/*.html
